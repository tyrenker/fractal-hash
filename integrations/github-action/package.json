{
  "name": "fractal-hash-action",
  "version": "1.0.0",
  "description": "GitHub Action: generate visual fractal fingerprints for release checksums",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && npx ncc build dist/index.js --license licenses.txt -o dist/ncc",
    "build:ts": "tsc"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/glob": "^0.4.0",
    "fractal-hash": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
